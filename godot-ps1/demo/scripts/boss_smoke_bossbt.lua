-- Placeholder — gets overwritten at export by SceneCollector's
-- RecompileSiblingGraphIfPresent, which reads the sibling
-- boss_smoke_bossbt.tres (BossBT graph) and emits the
-- `_G.bossbt_boss_smoke_bossbt` config table here.
--
-- DO NOT EDIT THIS FILE BY HAND — edit the .tres in the
-- PS1Graph dock or with a text editor (see RFC at
-- docs/internal/rfc/bossbt-graph-kind.md), then F5 to regenerate.
--
-- Consumed by boss_smoke_brain.lua via
--   Combat.MeleeBoss(_G.bossbt_boss_smoke_bossbt)
-- so this file just needs to ride along in the splashpack via
-- PS1Scene.UserScripts on the BossSmoke root node.
