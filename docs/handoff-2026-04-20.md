# Session handoff — 2026-04-20

Pick-up notes for continuing the demo work next session. State of the
repo as of this writing — see `git log` for the full chain.

## Where we are

Phase 2 status: bullets 1–6, 8 (MVP), 9, 10 (full A + B.1 + B.2 1/2/3)
all running on PSX. Bullet 11 (skinned meshes) and bullet 12
(rooms/portals) remain. Phase 2.5/2.6/3 plus the upstream-improvement
tracker capture everything else.

The Portal-style narrative demo is mostly working:
- Intro cutscene (8s → extended to 20s) with camera arc + system voice
  narration co-fired from Lua.
- Green Cube cycles through 3-line first conversation + 1-line
  "good talk" + 3 looping extras on Triangle press, with audio.
- Cube2 (textured) is also interactable + bouncing + has 4-line
  cycling dialog (`checkered_dialog.lua`).
- CheckeredZone proximity trigger with system voice ("Please ignore
  the checkered cube." → "It is not part of the test." after linger).
- HUD tick counter, dialog box, idle two-beat meta line.
- 20 voice clips wired (~450 KB ADPCM, fits SPU's ~508 KB).
- Humanoid mesh (FBX) imported + converted to a Player PS1MeshInstance
  via the new `Convert MeshInstance3D to PS1MeshInstance` tool menu.

## Open bugs reported but not yet fixed (priority order)

These are all from the user's most recent run (the one with the
humanoid in scene). The Godot-side export log was clean; the PSX-side
log wasn't pasted. Need to ask for it next session.

### 1. `Entity.SetPosition` signature was wrong (FIXED but not pushed)

`test_logger.lua` was calling `Entity.SetPosition(playerMesh, p.x, p.y, p.z)`
— the runtime takes a Vec3 table as the second arg, not 3 numbers.
Silent no-op via `if (!lua.isTable(2)) return 0;` left the player
mesh stuck at (0, 0, 0) regardless of runtime player position.

Fixed in `test_logger.lua` to `Entity.SetPosition(playerMesh, p)`. Stage
+ commit + push first thing.

### 2. Player mesh "way too large" on PSX

Humanoid renders at sane size in Godot's editor viewport but is huge
on PSX. Hypothesis: the PSXMesh exporter reads raw vertex positions
from `mesh.GetSurfaceArrays()[Vertex]` and divides by `gteScaling=4`,
but **doesn't apply the MeshInstance3D's transform scale**. If the FBX
imported with a non-1.0 internal scale (e.g. cm units → factor 100)
that's compensated by an instance transform scale on the Godot side,
the exporter sees the raw (huge) vertex values.

Fix candidate: in `PSXMesh.FromGodotMesh()`, multiply each vertex by
`node.Scale` (or `node.GlobalTransform.Basis.Scale`) before the
gteScaling divide. Plus emit a warning when `Scale != Vector3.One`
since combining that with affine warp can look weird.

File: `godot-ps1/addons/ps1godot/exporter/PSXMesh.cs:17` —
`FromGodotMesh()`.

### 3. No audio playing

Likely downstream of (1) — if `onCreate` raised an error before
`Cutscene.Play("intro")` ran, no audio events fire. After fixing the
SetPosition signature, audio should come back. **Need PSX log to
confirm.**

If audio still silent after (1) is fixed: check whether per-clip
loadClip is succeeding. The total ADPCM is ~452 KB which leaves ~67 KB
SPU margin — should fit. But there might be alignment overhead per
clip pushing it over.

### 4. Camera "wasn't attached to player"

Either the cutscene never released camera control (cutscene didn't
end?) or the runtime camera-follow logic isn't picking up the
PS1Player's spawn position correctly. The export log shows
`Player spawn from PS1Player 'PS1Player' at (0, 2, 8), mode=ThirdPerson`
— so the spawn made it into the splashpack. If camera follow is
broken, it's a runtime-side issue. Need PSX log to bisect.

### 5. Player "wasn't moving"

Could be:
- Input dead during the 20-second cutscene (expected — cutscene blocks
  controls)
- Lua error in `test_logger.lua` onUpdate that aborts the script and
  somehow tangles control state
- Input mapping change?

The cutscene is 600 runtime frames at 30 fps cutscene-time = 20 s of
"controls suppressed (camera tracks active)." User may have given up
before the cutscene ended. Worth shortening the cutscene back to
something tighter once narration is verified working.

## Next-session step list

1. **Commit + push the SetPosition fix** that's already in
   `test_logger.lua`.
2. **Ask for the PSX log** from the next run — that'll confirm whether
   audio + cutscene end + player movement come back once SetPosition
   is fixed.
3. **Bake instance scale into vertex export** in `PSXMesh.FromGodotMesh`
   (item 2 above). Test with the humanoid. The Godot-side log
   `Player → 402 tris` will stay the same, but the visible size on PSX
   should match what's in the editor.
4. **Cube2 humanoid texture** — `NonOverlapping.png` was 1024×1024,
   auto-downscaled to 256×256. Acceptable, but the humanoid will look
   blurry. Consider authoring at 128×128 or 256×256 from the source.
5. After the demo is solid, **bullet 11 (skinned meshes)** is the
   natural next port — the humanoid would then walk-cycle properly
   instead of T-posing while sliding. See `cutscene.hh`'s
   `CutsceneSkinAnimEvent` + `SkinAnimSet` for the runtime layout.

## Important context to remember

- **psxlua is integer-only.** No float literals (`0.00001`, `2.4`).
  Use `Convert.IntToFp(rawValue)` if you need fractional fp12 values.
- **tscn does NOT support `#` comments.** Adding `#` lines corrupts
  the parser and can drop nodes silently. Notes go in commit messages
  or adjacent .md files.
- **Entity.SetPosition takes a Vec3 table.** Same for any other
  function that takes a Vec3 — don't pass 3 numbers.
- **Default material `ps1_default.tres`** must stay texture-free. It
  gets auto-assigned to every new PS1MeshInstance via
  `_EnterTree()`. Floor uses the dedicated `ps1_floor_grass.tres`.
- **Cutscene.Play vs PS1AudioEvent timing.** The runtime's
  `m_frame` and Lua's onUpdate-tick advance at different rates
  (60 fps onUpdate vs 30 fps cutscene-frame). For text+audio sync
  during cutscenes, fire BOTH from Lua at the same `cutsceneFrame`
  counter (which only increments while `Cutscene.IsPlaying()`).
  Don't use PS1AudioEvent + separate Lua text reveals.
- **Demo layout right now**:
  - PS1Player spawn: `(0, 2, 8)` facing -Z
  - PS1TriggerBox: `(0, 1, 4)` HE=(3, 2, 1) — gate
  - Cube (green, spinning): `(-4, 1, -2)` interactable Triangle
  - Cube2 (checkered, bouncing): `(4, 1, -3)` interactable Triangle
  - CheckeredZone trigger: wraps Cube2
  - Player (humanoid, T-pose): tracks runtime player position via Lua

## Memory state

The session memory file at
`C:\Users\Cornelio\.claude\projects\D--Documents-JetBrains-PS1Godot\memory\project_phase1_resume_point.md`
is stale relative to the demo state — last updated when bullets 1-5
were verified. Worth refreshing next session with the bullet 6 / 8 /
9 / 10 status.

Memory entries for `feedback_caveman_commit_review.md` (commit cadence)
and `feedback_low_barrier_of_entry.md` (UX defaults) are still
accurate and load-bearing — keep applying them.

## Github

Repo: https://github.com/BuffJesus/PS1Godot
Last pushed commit: `c969345` (integer-only Lua math fix). The
SetPosition fix in `test_logger.lua` is local-only — push at
session start.
